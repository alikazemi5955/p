# قوانین دائمی و ساختار پروژه پازل کالا (Puzzle Kala)

> **توجه حیاتی برای کلیه Agentها:**  
> قبل از انجام هر کاری، حتماً فایل مرجع و دائمی `PROJECT_RULES.md` را مطالعه کرده و تمامی اصول نوزده‌گانه آن را با دقت اجرا کنید.

## اصل طلایی
«کمترین تغییر لازم، حفظ قابلیت‌های سالم، یک Source of Truth، حذف تمام فایل‌ها و پوشه‌های اضافی، پروژه سبک، و Deployment تمیز و به‌روز.»

## اصول کلیدی:
1. **یک نسخه اصلی (Single Source of Truth)**: هیچ کپی، backup، پوشه `_source_code`، یا آرشیو ZIP داخل پروژه نگهداری نشود.
2. **پوشه Deploy (`puzzlecod/`)**: تنها حاوی فایل‌های سبک و بهینه‌سازی‌شده برای اجرای Production روی هاست باشد؛ بدون سورس کد، بدون `node_modules` و بدون فایل‌های تودرتو.
3. **امنیت کامل Secrets**: هیچ رمز، کلید API یا نشست هاردکد نشود. همه متغیرها از طریق `process.env` خوانده شوند.
4. **حفظ کارایی و سلامت**: هیچ ویژگی یا API سالمی بدون درخواست مستقیم دستکاری یا حذف نشود.
5. **لوگوی اختصاصی و غیرقابل تغییر**: فایل تصویری آپلودشده توسط کاربر تنها منبع اصلی و دائمی لوگوی پروژه است (`puzzlecod/logo.png` و `logo.png`). هیچ Agent یا توسعه‌دهنده‌ای تحت هیچ شرایطی نباید این لوگو را بازطراحی کند، با AI مجدداً بسازد، رنگ یا ابعاد اصلی آن را دستکاری کند یا از لوگوی دیگری استفاده نماید.
