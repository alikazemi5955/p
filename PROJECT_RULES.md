# دستورالعمل کامل و دائمی پروژه «پازل کالا»

هدف:
پروژه را ساده، سبک، منظم، قابل نگهداری و آماده Deploy روی هاست نگه دار.
هر تغییر باید با کمترین دستکاری ممکن انجام شود و قابلیت‌های سالم قبلی حفظ شوند.

==================================================
1) قانون اصلی تغییرات
==================================================

برای هر تغییر جدید:

- ابتدا دقیقاً فایل و بخش مرتبط با درخواست را پیدا کن.
- فقط همان بخش لازم را تغییر بده.
- کل ساختار پروژه را بی‌دلیل بازنویسی نکن.
- فایل‌ها، APIها، صفحات و قابلیت‌های سالم را بدون دلیل جابه‌جا یا حذف نکن.
- قبل از تغییر فایل‌های مشترک، وابستگی‌های آن را بررسی کن.
- بعد از تغییر، بخش‌های نامرتبط را نیز بررسی کن تا چیزی خراب نشده باشد.

اگر تغییر بزرگ واقعاً ضروری بود:
- ابتدا دلیل و محدوده تغییر را مشخص کن.
- از تغییرات غیرضروری خارج از محدوده خودداری کن.

==================================================
2) فقط یک Source of Truth
==================================================

پروژه باید فقط یک نسخه اصلی از Source Code داشته باشد.

کدهای تکراری، backup، source-copy و نسخه‌های قدیمی نباید در پروژه نگهداری شوند.

به‌خصوص از ساختارهای زیر جلوگیری کن:

- public/puzzlecod/_source_code/
- public/puzzlecod.zip
- ZIP داخل ZIP
- چند نسخه از یک پروژه
- کپی کامل Source در public
- buildهای قدیمی و بلااستفاده

قبل از حذف هر فایل:
- بررسی کن واقعاً استفاده نمی‌شود.
- اگر فایل مورد نیاز Production است، آن را حذف نکن.
- اگر فقط کپی، backup، cache یا فایل موقت است، حذف شود.

==================================================
3) سبک نگه داشتن پروژه
==================================================

تمام اطلاعات و پوشه‌های اضافی باید حذف شوند، مشروط بر اینکه برای اجرای پروژه لازم نباشند.

مواردی که نباید داخل Source/Deployment نگهداری شوند:

- node_modules
- cache
- فایل‌های موقت
- backupهای غیرضروری
- ZIPهای داخلی
- ZIP داخل ZIP
- سورس تکراری
- buildهای قدیمی
- فایل‌های تستی که در Production لازم نیستند
- لاگ‌های قدیمی
- فایل‌های IDE یا سیستم‌عامل که برای پروژه لازم نیستند
- هر فایل اضافی که فقط حجم پروژه را بالا می‌برد

node_modules باید از بسته پروژه حذف شود و وابستگی‌ها از package.json / lockfile نصب شوند.

==================================================
4) پوشه puzzlecod برای هاست
==================================================

یک پوشه با نام دقیق:

puzzlecod

فقط در صورتی نگهداری شود که واقعاً برای Deployment لازم است.

این پوشه باید سبک باشد و فقط فایل‌های لازم برای اجرای نسخه Production روی هاست را داشته باشد.

اگر پوشه puzzlecod یا ZIP مربوط به آن بیش از حد سنگین است:
- فایل‌های غیرضروری آن را شناسایی کن.
- فایل‌های اضافی را حذف کن.
- node_modules را داخل آن قرار نده.
- ZIP تودرتو قرار نده.
- Source Code کامل و تکراری را داخل آن قرار نده.
- cache و فایل‌های موقت را قرار نده.
- buildهای قدیمی را قرار نده.

اگر مشخص شد puzzlecod هیچ کاربرد ضروری ندارد، ساختار اضافی آن را حذف یا ساده کن؛ اما قبل از حذف فایل‌های مورد نیاز هاست، وابستگی Deployment را بررسی کن.

==================================================
5) Production Deployment Package
==================================================

puzzlecod را به‌عنوان بسته Production در نظر بگیر.

قاعده:

Source اصلی
    ↓
Build
    ↓
puzzlecod
    ↓
Upload to hosting

بعد از هر تغییر مهم:
- اگر Production تغییر کرده، puzzlecod نیز به‌روز شود.
- نباید Source جدید باشد ولی بسته Production نسخه قدیمی داشته باشد.

بسته Production باید حداقل فایل‌های لازم برای اجرای سایت را داشته باشد، نه کل پروژه توسعه.

==================================================
6) ZIP نهایی
==================================================

اگر ZIP برای انتقال یا آپلود ساخته شد:

ZIP باید تمیز و سبک باشد.

داخل آن نباشد:

- node_modules
- ZIP دیگر
- پروژه تکراری
- backup غیرضروری
- cache
- فایل‌های موقت
- buildهای قدیمی
- فایل‌های توسعه‌ای غیرضروری

هرچه برای اجرای Production لازم نیست، در Deployment ZIP قرار نگیرد.

==================================================
7) ساختار ساده پیشنهادی
==================================================

ساختار نهایی را تا حد ممکن ساده نگه دار.

نمونه:

project/
├── src/                  ← Source اصلی Frontend
├── server/               ← Source اصلی Backend، اگر وجود دارد
├── public/               ← فایل‌های عمومی لازم
├── puzzlecod/            ← فقط Deployment Production در صورت نیاز
├── package.json
├── package-lock.json     ← یا lockfile مربوط به package manager
├── tsconfig.json
├── vite.config.*         ← در صورت استفاده
├── PROJECT_RULES.md
└── README.md             ← در صورت نیاز

ساختار دقیق را بر اساس معماری واقعی پروژه تعیین کن و پوشه اضافی نساز.

==================================================
8) TypeScript و Build
==================================================

tsconfig باید فقط Source واقعی پروژه را بررسی کند.

نباید:
- backupها
- source-copyها
- فایل‌های داخل Deployment که Source نیستند
- node_modules
- فایل‌های generated غیرضروری

به‌صورت اشتباه وارد compilation شوند.

بعد از تغییرات ساختاری بررسی کن:

npm run lint
npm run build

اگر این دستورات در package.json وجود دارند.

خطاها را فقط در محدوده لازم اصلاح کن.

==================================================
9) امنیت
==================================================

هیچ Secret واقعی داخل Source Code یا ZIP قرار نگیرد.

مواردی مثل:

- GEMINI_API_KEY
- SESSION_SECRET
- password
- token
- credentials

باید از Environment Variables / Secrets خوانده شوند.

.env واقعی و اطلاعات حساس نباید در بسته عمومی یا ZIP Production قرار بگیرند.

.env.example فقط می‌تواند نام متغیرها را داشته باشد، نه Secret واقعی.

اگر در کد fallback برای SESSION_SECRET یا Secret حساس وجود دارد، بررسی کن که در Production به Secret واقعی وابسته باشد و Secret ثابت ناامن داخل کد باقی نماند.

==================================================
10) Gemini
==================================================

Gemini باید از سمت Server استفاده شود، اگر معماری فعلی پروژه همین است.

API Key نباید در Frontend قرار گیرد.

GEMINI_API_KEY از Environment/Secrets خوانده شود.

این معماری را بدون دلیل تغییر نده.

==================================================
11) Authentication و API
==================================================

Authentication، Session، APIها و Database/Data را فقط در صورت ارتباط مستقیم با درخواست تغییر بده.

قبل از تغییر:
- محل تعریف API را پیدا کن.
- محل مصرف API در Frontend را پیدا کن.
- مدل داده و authentication را بررسی کن.
- بعد فقط قسمت لازم را اصلاح کن.

هیچ API سالمی بدون دلیل حذف یا تغییر نکند.

==================================================
12) داده‌ها
==================================================

داده‌های واقعی پروژه را بدون دلیل حذف نکن.

اما فایل‌های:
- تستی
- نمونه‌ای
- backup
- duplicate
- temporary

که برای اجرای Production لازم نیستند، حذف شوند.

قبل از حذف فایل داده، بررسی کن که برنامه واقعاً از آن استفاده نمی‌کند.

==================================================
13) مستندسازی دائمی
==================================================

این قوانین باید در فایل:

PROJECT_RULES.md

باقی بمانند.

هر بار که پروژه باز می‌شود یا تغییر مهمی انجام می‌شود، ابتدا PROJECT_RULES.md را بخوان.

این فایل باید مستقل از Gmail یا Google AI Studio باشد تا با تغییر حساب، قوانین پروژه از بین نروند.

==================================================
14) روند کار برای هر درخواست جدید
==================================================

برای هر درخواست:

مرحله 1:
درخواست را دقیق مشخص کن.

مرحله 2:
فایل‌ها و وابستگی‌های مرتبط را پیدا کن.

مرحله 3:
کمترین محدوده تغییر را تعیین کن.

مرحله 4:
فقط همان قسمت را اصلاح کن.

مرحله 5:
Build/Lint یا تست مرتبط را اجرا کن.

مرحله 6:
بررسی کن قابلیت‌های نامرتبط خراب نشده باشند.

مرحله 7:
اگر Production تغییر کرده، puzzlecod را به‌روز کن.

مرحله 8:
فایل‌های اضافی ایجادشده را پاک کن.

مرحله 9:
ساختار پروژه را دوباره تمیز بررسی کن.

==================================================
15) اصل جلوگیری از بزرگ شدن پروژه
==================================================

هر تغییر جدید نباید باعث ایجاد نسخه دوم از فایل یا پروژه شود.

از این کارها خودداری کن:

- file-final
- file-final2
- backup
- backup-new
- old
- copy
- copy2
- project-new
- ZIP داخل ZIP
- Source داخل Source
- build داخل build

اگر نسخه قبلی لازم نیست، نگه داشته نشود.

==================================================
16) قانون مخصوص Google AI Studio
==================================================

به دلیل محدودیت حجم/context و احتمال محدود شدن پردازش:

- پروژه را تا حد ممکن کوچک نگه دار.
- فایل‌های غیرضروری را وارد پروژه نکن.
- Source تکراری نداشته باش.
- node_modules را ارسال نکن.
- ZIP داخلی نداشته باش.
- فایل‌های generated غیرضروری را حذف کن.
- هنگام هر درخواست، فقط فایل‌های مرتبط را تغییر بده.
- از بازنویسی کل پروژه برای یک تغییر کوچک خودداری کن.

==================================================
17) قبل از حذف فایل
==================================================

هیچ فایل مهمی را صرفاً به دلیل نام یا حجم حذف نکن.

برای فایل‌های بزرگ یا پوشه‌های بزرگ:
1. بررسی کن چه چیزی داخل آن است.
2. مشخص کن آیا Runtime به آن نیاز دارد.
3. مشخص کن آیا Build به آن نیاز دارد.
4. مشخص کن آیا Deployment به آن نیاز دارد.
5. اگر فقط duplicate/cache/backup است، حذف کن.

هدف:
کمترین حجم ممکن بدون خراب کردن قابلیت‌های واقعی پروژه.

==================================================
18) هدف نهایی
==================================================

پروژه باید:

- ساده باشد
- منظم باشد
- یک Source of Truth داشته باشد
- بدون کد تکراری باشد
- بدون فایل‌های اضافی باشد
- Build قابل اعتماد داشته باشد
- Secret امن داشته باشد
- برای AI Studio سبک باشد
- Production package سبک داشته باشد
- puzzlecod در صورت نیاز همیشه به‌روز باشد
- با تغییر Gmail نیاز به توضیح دوباره معماری نداشته باشد

==================================================
19) قانون قطعی و دائمی لوگوی اختصاصی پروژه
==================================================

فایل تصویری لوگوی آپلودشده توسط کاربر، تنها منبع رسمی، اصلی و دائمی لوگوی پروژه «پازل کالا» است.

این فایل تصویری در مسیرهای زیر قرار دارد:
- `puzzlecod/logo.png`
- `puzzlecod/favicon.png`
- `logo.png`
- `dist/logo.png`
- `dist/favicon.png`

قوانین قطعی و غیرقابل تغییر:
1. هیچ هوش مصنوعی (AI) یا برنامه‌نویسی حق بازطراحی (Redesign)، تولید مجدد با AI، ایجاد تغییر در رنگ‌ها، متون، فرم یا ابعاد هندسی این لوگو را ندارد.
2. هیچ لوگوی دیگری نباید جایگزین این فایل شود.
3. در تمام بخش‌های برنامه (هدر، فوتر، صفحه ورود و عضویت، صفحات عمومی، پنل مدیریت، فاوآیکون و مانیفست) باید دقیقاً همین فایل لود شود.
4. این فایل در پکیج استقرار پروداکشن (`puzzlecod/`) همواره باید حفظ شده و آماده Deploy باشد.

اصل طلایی:

«کمترین تغییر لازم، حفظ قابلیت‌های سالم، یک Source of Truth، حذف تمام فایل‌ها و پوشه‌های اضافی، پروژه سبک، و Deployment تمیز و به‌روز.»
